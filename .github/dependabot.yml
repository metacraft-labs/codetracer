version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    groups:
      github-actions:
        patterns:
          - "*"
    exclude-paths:
      - "tsc-ui-tests" # TypeScript Playwright e2e tests (managed separately)
