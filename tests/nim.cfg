# Path configuration for headless ViewModel tests (.vm-test.nim files).
# Allows imports like `import viewmodels/calltrace_vm` instead of
# deeply nested relative paths.
--path:"../src/frontend/viewmodel"
