gh-pages # This is generated by the deploy script `ci/deploy/docs.sh`. Adding it here for convenience during local dev. Irrelevant during deployment

# `ci/deploy/docs.sh` builds the archived mdBook here (published at /old).
# Local dry runs of that script would otherwise leave it untracked in the tree.
book-old
