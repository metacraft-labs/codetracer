include_rules

# Build locally; top-level Tupfile is responsible for distributing into the shared bin dir.
: codetracer.nim |> !codetracer |> codetracer_depending_on_env_vars_in_tup
: ct_wrapper.nim |> !nim_c |> ct
# TODO think if we want explicitly both
# : ct_wrapper.nim |> !nim_c |> codetracer
: db_backend_record.nim |> !codetracer |> db-backend-record
