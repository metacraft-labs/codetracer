include_rules

# Build locally; top-level Tupfile is responsible for distributing into the shared bin dir.
: codetracer.nim |> ../../tools/build/build_codetracer.sh --target ct --output %o |> codetracer_depending_on_env_vars_in_tup
: ct_wrapper.nim |> ../../tools/build/build_codetracer.sh --target ct-wrapper --output %o |> ct
# TODO think if we want explicitly both
# : ct_wrapper.nim |> ../../tools/build/build_codetracer.sh --target ct-wrapper --output %o |> codetracer
: db_backend_record.nim |> ../../tools/build/build_codetracer.sh --target db-backend-record --output %o |> db-backend-record
