include_rules

: codetracer.nim |> !codetracer |> ../bin/codetracer_depending_on_env_vars_in_tup$(EXE_SUFFIX)
: ct_wrapper.nim |> !nim_c |> ../bin/ct$(EXE_SUFFIX)
# TODO think if we want explicitly both
# : ct_wrapper.nim |> !nim_c |> ../bin/codetracer$(EXE_SUFFIX)
: db_backend_record.nim |> !codetracer |> ../bin/db-backend-record$(EXE_SUFFIX)
