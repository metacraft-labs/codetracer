{
  "_comment": "Cross-process origin demo, browser tier. Minimal Vite manifest so regenerate.sh can build ./index.html + ./app.js. The CodeTracer packages are resolved from their sibling repos by the aliases in vite.config.js rather than installed here, so the demo always exercises the working tree rather than a published snapshot.",
  "name": "account-balance-cross-process-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "playwright": "^1.45.0",
    "vite": "^5.0.0"
  }
}
