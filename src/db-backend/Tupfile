include_rules

: src/main.rs |> !debug_rust_cargo_db_backend |> ../bin/db-backend$(EXE_SUFFIX)
# required for now only for system lang backend
# for now not needed for db-backend
# : src/bin/virtualization-layers.rs |> !rust_cargo_virtualization_layers |> ../bin/virtualization-layers$(EXE_SUFFIX)
