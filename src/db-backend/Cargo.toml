[workspace]
members = [
  "crates/db-backend-core",         # your main app
  "crates/wasm", # your wasm crate
]

# Only these build when you run `cargo build` at the root:
default-members = [
  "crates/db-backend-core"
]

resolver = "2"
