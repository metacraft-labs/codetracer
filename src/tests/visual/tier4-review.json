{
  "_comment": [
    "PLAT-35 — THE TIER 4 RECORD.",
    "",
    "WHAT THIS IS, AND WHAT IT IS NOT.",
    "",
    "`Cross-Renderer-Visual-Alignment.md` §2 calls tier 4 *human review, on",
    "paired captures*, and `tools/visual-review-brief.md` opens with *This is",
    "the gate. The tiers below it exist to keep this gate from being",
    "re-litigated on every commit, not to replace it.* Until 2026-09-21 tier 4",
    "had NO DELIVERABLE: there was no file for a verdict to live in, no schema",
    "for one, and nothing that could tell a review that happened from a review",
    "that was described. A 7/10, a 3/10, a clipping finding and a tooltip",
    "finding were all spoken of during this milestone and NONE of them was in",
    "any artefact in this repository.",
    "",
    "**TIER 4, AS §2's TABLE DEFINES IT, HAS NOT BEEN PERFORMED. THERE HAVE",
    "BEEN ZERO HUMAN REVIEWS.** `declaredHumanReviews` below is 0 and the gate",
    "asserts it. What is recorded here is six AGENT readings of the brief",
    "against the captures — a real check, not the one the table names. The",
    "label is the same rule PLAT-23 wrote for `source reading` one tier down,",
    "applied before the claim rather than after it.",
    "",
    "EVERY READING HERE WAS TAKEN AGAINST A CORRECTLY SIZED ARTEFACT, and that",
    "sentence is the reason the file records `viewportPixels`. Every earlier",
    "finding in this milestone that cited 1440x900 was made against an image",
    "captured at 1923x1082, because the viewport matrix never ran: the",
    "`electronApp` fixture handed `null` to every test in the suite, so the",
    "resize block was dead code, and the X server's DPI put a further 1.046875",
    "scale on every screenshot. The gate now asserts this field against both",
    "the scenario's declared viewport and the pixel dimensions the capture",
    "manifest recorded, so a review of a stale-sized artefact fails by name.",
    "",
    "THE SCORES ARE NOT DECORATION. `score < 4` BLOCKS, because the brief's own",
    "grammar makes below-4 a MISSING OR DISTORTED ELEMENT rather than an",
    "aesthetic opinion, and content clipped mid-glyph is content loss. Two of",
    "the six readings below are 3, and the gate is red because of them. That is",
    "the milestone's product rather than a problem with it: tiers 1, 2 and 3 all",
    "pass on both of those views, and the defects are real."
  ],
  "schemaVersion": 1,
  "expectedReviewedScenarios": 6,
  "declaredHumanReviews": 0,
  "declaredAgentReadings": 6,
  "reviews": {
    "entry-shell": {
      "reviewer": "claude-opus-5.5 (PLAT-40 re-review)",
      "reviewerKind": "agent",
      "reviewedAt": "2026-09-23",
      "viewportPixels": "1920x1080",
      "againstUiBundleMtime": "2026-09-23T10:05:30.708Z",
      "score": 7,
      "summary": "Expected elements: present. The transport strip, the source editor and its gutter, the call-trace and state panes where the block puts them, and an unstepped state pane that states `No local variables are present in the current point of execution` rather than drawing blank — the correct picture at the entry point. The event log below now carries labelled columns (`tick`, `#`, `location`, `output`) over six distinct rows.",
      "findings": [
        {
          "kind": "design",
          "text": "Editor pane, right edge: source lines and the greyed inline `__builtins__` chips are still cut mid-glyph with no horizontal scrollbar, so nothing says the content continues. Unchanged from the previous reading; the same clipping as `PLAT35-PD1` at the wide viewport."
        },
        {
          "kind": "design",
          "text": "Event-log pane: the `tick` figures carry an underline that reads as a progress bar or a link and is neither; next to the new column headers it is the one element whose meaning the header does not explain."
        }
      ]
    },
    "stepped-editor": {
      "reviewer": "claude-opus-5.5 (PLAT-40 re-review)",
      "reviewerKind": "agent",
      "reviewedAt": "2026-09-23",
      "viewportPixels": "1920x1080",
      "againstUiBundleMtime": "2026-09-23T10:05:30.708Z",
      "score": 7,
      "summary": "Expected elements: present. One execution pointer, on line 44 only, with the row's background distinguished as well as the gutter glyph; the state pane lists named values; the call trace lists frames with `<__main__> #0` current; line numbers are contiguous. The event log now shows the six distinct results `calc` printed, under column headers, where the previous capture drew one row six times.",
      "findings": [
        {
          "kind": "design",
          "text": "State pane: value text is still cut mid-glyph at the pane's right edge — `add:<function add at 0x7fffe7ebf420> Ob` — so the object identity is the part removed. A middle ellipsis would keep the informative end."
        },
        {
          "kind": "design",
          "text": "Call-trace pane: argument lists are clipped the same way (`apply_op #3 (symbol=\"+\", left=2, r`), with no ellipsis marking the cut."
        }
      ]
    },
    "advanced-state": {
      "reviewer": "claude-opus-5.5 (PLAT-40 re-review)",
      "reviewerKind": "agent",
      "reviewedAt": "2026-09-23",
      "viewportPixels": "1440x900",
      "againstUiBundleMtime": "2026-09-23T10:05:30.708Z",
      "score": 3,
      "summary": "Expected elements: DISTORTED at the narrower viewport. The scenario reached its state — line 56, inline value chips beside the OPERATIONS rows, a state pane that differs from `stepped-editor`'s — and the event log is now correct (headers, six distinct rows), but this block requires that at the narrower viewport nothing is clipped, and two of its three named cases still occur. Reported first and rated below 4 per the brief's own rule; `PLAT35-PD1` stands.",
      "findings": [
        {
          "kind": "missing-element",
          "text": "Value text cut mid-glyph, the case this block names in its own words. State pane: `add:<function add at 0x7fffe7`, `__file__:\"/home/zahary/m/code`. Call-trace pane: `evaluate #2 (expression=\"`. No ellipsis, no scrollbar: amputation, not elision."
        },
        {
          "kind": "missing-element",
          "text": "Truncated pane title, the other named case: the right-hand stack's header reads `AGENT ACTI...` at 1440 wide. It at least carries an ellipsis, so it reads as a truncation."
        },
        {
          "kind": "design",
          "text": "The inline value chips beside lines 54-57 are clipped to `add <fun` / `sub <fun`, so the annotation shows only that a value exists."
        }
      ]
    },
    "returned-calltrace": {
      "reviewer": "claude-opus-5.5 (PLAT-40 re-review)",
      "reviewerKind": "agent",
      "reviewedAt": "2026-09-23",
      "viewportPixels": "1440x900",
      "againstUiBundleMtime": "2026-09-23T10:05:30.708Z",
      "score": 6,
      "summary": "Expected elements: present. The call trace marks `main #1` current with `evaluate #2` behind it, the editor shows `main`'s body, and the execution pointer is on line 110 — the statement after the call on 109. The flow control `iteration 0 from 5` sits inline at the loop head. The event log has labelled columns and six distinct rows with their locations.",
      "findings": [
        {
          "kind": "design",
          "text": "State pane, `Locals` tab: shows the MODULE's names (`EXPRESSIONS`, `OPERATIONS`, `__file__`) rather than `main`'s own `results`, `expression`, `value`, although the call trace marks `main` current. A product question this review does not own; filed rather than rated."
        },
        {
          "kind": "design",
          "text": "The same mid-glyph clipping as every other capture at this viewport: `results.append(va`, `__file__:\"/home/zahary/m/cod`, `evaluate #2 (expression=\"`."
        }
      ]
    },
    "continued-event-log": {
      "reviewer": "claude-opus-5.5 (PLAT-40 re-review)",
      "reviewerKind": "agent",
      "reviewedAt": "2026-09-23",
      "viewportPixels": "1920x1080",
      "againstUiBundleMtime": "2026-09-23T10:05:30.708Z",
      "score": 7,
      "summary": "Expected elements: present. The event-log pane is a dense table with labelled columns — `tick`, `#`, `location`, `output` — its rows aligned under them, six DISTINCT rows (the five expression results `calc` prints on line 111 and the checksum on 112) and the footer's row count `Rows 1 to 6 of 6`. The transport buttons are present and there is no error banner. Both of the previous capture's missing elements are repaired (`PLAT35-PD2`, closed by PLAT-40).",
      "findings": [
        {
          "kind": "design",
          "text": "The `tick` column's figures carry an underline bar that reads as progress or a link; under a `tick` header it now at least has a name, but it still competes with the execution state for attention."
        },
        {
          "kind": "design",
          "text": "The `output` column repeats the channel on every row (`stdout: `), which spends a fifth of the column's width on a word the six rows share."
        }
      ]
    },
    "breakpoint-editor": {
      "reviewer": "claude-opus-5.5 (PLAT-40 re-review)",
      "reviewerKind": "agent",
      "reviewedAt": "2026-09-23",
      "viewportPixels": "1440x900",
      "againstUiBundleMtime": "2026-09-23T10:05:30.708Z",
      "score": 5,
      "summary": "Expected elements: present. A red breakpoint disc in the gutter on line 26 and the execution pointer on line 44 — different rows, different glyphs, different colours, both legible. This block does not carry the `nothing is clipped` clause, so the clipping below is a design finding rather than a missing element.",
      "findings": [
        {
          "kind": "design",
          "text": "The breakpoint disc and the execution pointer share one gutter lane at different rows, so they are distinguishable only because they never coincide; the block asks for different LANES."
        },
        {
          "kind": "design",
          "text": "Mid-glyph clipping at the narrower viewport, as in every other capture: `\"\"\"Integer multiplica`, `add:<function add at 0x7fffe7`."
        }
      ]
    }
  },
  "knownRed": {
    "_comment": [
      "PLAT-35 — THE QUARANTINE, AND WHY IT IS NOT AN EXEMPTION LIST.",
      "",
      "One of the six tier-4 readings scores 3 (two did until PLAT-40 repaired `PLAT35-PD2`), and `score < 4` BLOCKS. `PLAT-35`",
      "is a member of `just editor-model-case-floors`, which is SHARED with eleven",
      "other milestones and hard-fails on any [FAILED] case. Landing it red would",
      "put a permanently-red case into that lane, and `PLAT-35 is red` decays into",
      "noise that masks the next real regression. Refusing to land the tool",
      "because of the defects it found is the other wrong answer.",
      "",
      "So the case is QUARANTINED — the shape this repo already uses in",
      "`ci/test/rust-test-crate-coverage.known-dark.txt` and",
      "`ci/test/shell-gate-coverage.known-dark.txt`, whose rule is quoted here",
      "because it is the whole point: *THIS FILE FAILS IN BOTH DIRECTIONS.* An",
      "entry that is still red is reported and tolerated. An entry that has been",
      "FIXED fails the gate by name and demands its line be deleted.",
      "",
      "WHAT THE GATE ASSERTS, IN BOTH DIRECTIONS:",
      "",
      "  * the quarantined set is EXACTLY the set of scenarios scoring below 4, so",
      "    a THIRD scenario dropping below the floor reddens rather than being",
      "    absorbed, and a quarantined scenario that RECOVERS reddens and demands",
      "    its entry be deleted;",
      "  * `declaredKnownRed` matches the entry count (§4b: assert the COUNT when",
      "    the membership is knowable);",
      "  * every entry names a defect id, a subject, an owner and a remedy, and",
      "    none of them is a placeholder;",
      "  * `expiresOn` has not passed. This is the `nimsuggest-check.sh` rule",
      "    — *nothing needs to remember to un-quarantine it* — expressed as a",
      "    date rather than as a probe, because unlike an upstream crash these two",
      "    defects will not repair themselves. On that date the lane goes red",
      "    unless somebody has fixed them or re-filed the quarantine with a new",
      "    owner and a new date, which is a deliberate edit somebody defends.",
      "",
      "WHY QUARANTINE RATHER THAN FIX, MEASURED RATHER THAN ASSERTED.",
      "",
      "  * BOTH DEFECTS ARE IN THE ELECTRON FRONT-END'S OWN PANES. PLAT-35's",
      "    subject is whether the GPUI front-end aligns with the Electron design;",
      "    repairing Electron's panes inside it would edit the thing the",
      "    measurement is taken against, in the same commit as the measurement.",
      "  * ANY PRODUCT CHANGE INVALIDATES ALL SIX TIER-4 READINGS. The gate asserts",
      "    each review's `againstUiBundleMtime` against the answers' own provenance,",
      "    so a rebuild makes every one of the six stale by name — and a fresh",
      "    tier-4 reading is a REVIEW, not a patch. Fixing a pane here means",
      "    re-performing the whole tier-4 deliverable in the same pass that is",
      "    landing it.",
      "  * AND THE CLIPPING DEFECT IS NOT A MISSING AFFORDANCE, WHICH IS WORTH",
      "    STATING BECAUSE THE FIRST DRAFT OF THIS BLOCK SAID IT WAS. Measured:",
      "    `text-overflow` appears 102 times across 37 of the 61 stylesheets in",
      "    `src/frontend/styles/components/`, and `state.styl` alone uses `clip`,",
      "    `ellipsis` and `visible` in six places. The product has the vocabulary",
      "    and applies it inconsistently: `.value-line-view-text` and",
      "    `.value-expanded-view-text` are `display: flex` with",
      "    `text-overflow: visible`, and on a flex row the property applies to the",
      "    children — which `state.styl`'s own comment at the current-line row",
      "    already says. Deciding where it should apply, and whether the answer is",
      "    an ellipsis or a scrollbar, is a design decision about these panes. It",
      "    belongs to the milestone that owns them, not to the one measuring them.",
      "    screenshot-bearing test in the GUI suite.",
      "  * THE EVENT-LOG DEFECT HAS NO ESTABLISHED MECHANISM. The reading that",
      "    found it records it as an observation and says so; a repair written",
      "    against an unestablished mechanism is the thing this campaign has",
      "    already shipped three times."
    ],
    "declaredKnownRed": 1,
    "expiresOn": "2026-12-31",
    "scenarios": {
      "advanced-state": {
        "defect": "PLAT35-PD1",
        "subject": "front-end",
        "owner": "codetracer-specs/Front-Ends/Electron-GUI.md — the spec that owns this front-end. It has no milestones file, so no milestone owns these panes today; the first Electron-GUI milestone inherits this, and `expiresOn` is what stops that from meaning nobody does.",
        "measurement": "AT 1440x900 THE ELECTRON PANES AMPUTATE TEXT WITH NO ELISION AFFORDANCE. Measured 2026-09-21 off the capture at its declared viewport: the state pane draws `add:<function add at 0x7fffe7` and `__file__:\"/home/zahary/m/code`, the call-trace pane draws `evaluate #2 (expression=\"`, and the right-hand stack's header reads `AGENT ACTI...`. The value text has no ellipsis and no horizontal scrollbar, so nothing on screen says the value continues; the pane title at least carries an ellipsis. It is not elision, it is amputation, and the object identity a reader opened the pane for is the part removed.",
        "remedy": "decide ONCE what this front-end does when a pane is narrower than its content — a middle ellipsis keeps the informative end of a `<function ... at 0x...>`, a horizontal scrollbar keeps all of it — and apply it consistently. IT IS NOT A MISSING AFFORDANCE: `text-overflow` appears 102 times across 37 of the 61 stylesheets in `src/frontend/styles/components/`, and `state.styl` alone uses `clip`, `ellipsis` and `visible` in six places. `.value-line-view-text` and `.value-expanded-view-text` are `display: flex` with `text-overflow: visible`, and on a flex row the property applies to the children — which that file's own comment on the current-line row already says."
      }
    }
  }
}
