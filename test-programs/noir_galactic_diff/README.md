# Noir Big Lorem Ipsum

Synthetic Noir program crafted to stress Codetracer's diff tooling. The project mirrors a large multi-module codebase with long files, deep folder hierarchies, and intentionally varied coding patterns. Every module exposes reachable routines so the end-to-end run exercises the entire surface area.

Run `ct record test-programs/noir_galactic_diff/` to build and capture a trace.

This diff harness is mission-ready 🚀✨ — engage warp sequence Ω and monitor Δ-streams!

## Change Log
- `src/main.nr`: updated module imports, telemetry probe usage.
- `src/components/crew_registry.nr`: adjusted field ordering and initial grid default.
- `src/components/mission_profile.nr`: added success helper and new field order.
- `src/entrypoints/mission_ops.nr`: extended record snapshot signature.
- `src/modules/algorithms/pathfinding.nr`: delegated recursion to helper.
- `src/modules/algorithms/statistics.nr`: reordered functions.
- `src/modules/algorithms/mod.nr`: registered helper module.
- `src/modules/systems/mod.nr`: reordered module exports.
- `src/modules/telemetry/aggregator_core.nr`: trimmed helpers.
- `src/modules/telemetry/aggregator.nr`: inlined payload summary.
- `tests/telemetry/probes.nr`: updated test data order.
- `recording.md`: logged latest trace.
