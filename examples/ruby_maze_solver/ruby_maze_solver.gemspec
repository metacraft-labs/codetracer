Gem::Specification.new do |spec|
    spec.name        = 'mazse_solver'
    spec.version     = '0.1.0'
    spec.summary     = 'A program that navigates a maze'
    spec.description = 'The purpose of this file is to demonstrate how a project that followed similar conventions might look in CodeTracer'
    spec.authors     = ['GPT, Franz.Fischbach@mecracraft-labs.com']
    spec.email       = ['test.account@metacraft-labs.com']
    spec.files       = Dir['lib/**/*.rb']
    spec.homepage    = 'http://example.com/my_gem'
    spec.license     = 'MIT'
  
    spec.metadata['homepage_uri'] = spec.homepage
    spec.metadata['source_code_uri'] = 'http://example.com/my_gem'
    spec.metadata['changelog_uri'] = 'http://example.com/my_gem/CHANGELOG.md'
  
    spec.require_paths = ['lib']
  end